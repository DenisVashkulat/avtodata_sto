const $=id=>document.getElementById(id);function val(v){const n=Number(String(v).replace(',','.').replace(/[^0-9.]/g,''));return Number.isFinite(n)&&n>0?Math.round(n*100)/100:null;}$('create').onclick=()=>{const a=val($('amount').value);if(!a){$('status').textContent='Введи правильну суму.';return;}const u=new URL('pay.html',location.href);u.searchParams.set('amount',a.toFixed(2));u.searchParams.set('purpose',$('purpose').value.trim()||'Оплата за ремонт автомобіля');$('link').value=u.href;$('result').classList.remove('hidden');$('status').textContent='';};$('copy').onclick=async()=>{await navigator.clipboard.writeText($('link').value);$('status').textContent='Посилання скопійовано.';};$('open').onclick=()=>location.href=$('link').value;